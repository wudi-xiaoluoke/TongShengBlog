
## 09:25 第十二轮：四连功能（commit a540272，113/113 测试）
1. 手机入口纯图标：去掉"店长手机"文字，CSS 重绘手机图标（机身描边+渐变屏+灵动岛点+home 条），aria-label 保留。
2. 游戏内部时间：`game-config.gameClock(state)` 把营业日 3 分钟映射为 09:00~21:00；手机时钟/账本 timeLabel 全用游戏时间。账本 recordLedger 存 timeLabel（旧存档条目无该字段则不显示）。
3. 货架每行换品：`state.shelfPlan`（柜数×4 二维数组，null=未自定义）；`shopPlanogram` 优先用 shelfPlan（不足柜数时自动轮转补齐新柜）；`swapShelfRow(state,slot,row,productId)` 物化排面+失去所有行的商品退回仓库；货架气泡底部新增 4 个行 select（unlockedProducts 选项），change → onSwapShelfRow → buildWorld+setWorld 热替换。
4. 结账圆环：顾客进入 checkout 时记录 `checkoutDurationMs`，game-scene 在头顶画底环+金色进度弧+¥ 符号（arc 起始角 -PI/2 顺时针）。测试踩坑：**mock ctx 的 arc(x,y,r,start,end)，断言角标是 args[3]/args[4] 不是 [2]/[3]**。
- harness 教训：iframe setItem+reload 在 headless 虚拟时间下不可靠（seed 不生效导致 level 1 下拉框没有目标选项）。**可靠方案：同源中间页写 localStorage 后 location.replace('/game')**，游戏 module 首次执行即读到种子。
- 验证：seed 后截图——金币 1992/等级 38/第 5 天，手机时钟 15:00（=9:00+90s/180s*12h），换品后第 1 柜第 1 行变黑巧克力、save-status"货架已重新摆放并存档"。
# 2026-09-05

## 四季+五时段系统（commit abebfd4，122/122 测试）
- 用户拍板：① 季节跟真实月份（3-5春/6-8夏/9-11秋/12-2冬，seasonForDate）；② 凌晨只做订货过场（dayBreak 冻结计时 → 夜幕 overlay 订货 → 点"开门营业"才 advanceDay）。
- 新增：game-config DAY_PHASES/SEASONS/dayPhaseFor/spawnIntervalMs/maxCustomersFor；state.season 缓存 + refreshSeason + dayBreak；main 打烊拦截 enterDayBreak/openNewDay + 客群加权 pickVariant；scene 时段窗景/挂钟走游戏时间/tint/粒子/冬雪。
- **大坑：同一文件多个 Edit 并行调用会互相覆盖（快照竞态），index.html 6 处编辑只活 3 处、game-ui.mjs 也丢块 → 模块 ReferenceError 白屏。教训：同一文件必须串行 Edit，改完必须 grep 校验全部标记。**
- 次坑：夜幕 overlay 用 opacity 入场动画在 headless 虚拟时间冻中间帧（旧教训重演）→ 改 transform。
- 验证：seed 中间页写 localStorage 跳 /game，5 场景截图（春晨/夏午/秋暮/冬夜/凌晨过场）全通过后删除 verify/。

## 打烊过场小票风重构（commit 1b85bdf，122/122 测试）
- 用户嫌结算页丑 → 重做：全屏 fixed 夜幕（月光晕/星星/云/天际线剪影）+ 奶油色收银小票（四格数据/目标进度条/红印章/日出渐变开门按钮）。
- **关键修复链**：① 卡片定高 min(94%,560px) 会占满舞台把按钮顶出视口 → 改 height:auto + max-height:min(92vh,620px) + 列表区 max-height:34vh 内滚；② overlay 从舞台内 absolute 升级为 fixed + showNightBreak 时 appendChild(body)（防 transform/filter 祖先劫持包含块）。
- **重大发现（纠正旧认知）**：headless --screenshot 的画面高度=--window-size，但布局视口 innerHeight 比 window-size 小约 93px → fixed 全屏 overlay 截图底部总会露一条 body 背景奶油条，**是截图伪影不是 bug**。判别方法：静态化 /game HTML 注入探针脚本量 getComputedStyle+getBoundingClientRect+innerHeight（dump-dom 看 title；iframe 探针在虚拟时间下 onload 时序不可靠，别用）。
- 若页面/游戏页不再单独链接 index.css 之外的站点样式，注意 body 背景色是奶油色，容易和伪影混淆。

## 打烊过场改手账双页（commit 49ce08a，122/122 测试）
- 用户拿手账页截图要求同款 → 夜幕里摊开一本双页账：复用 notes.css 视觉配方（封皮 #bd7a57→#8e5035 渐变包边 8px、中缝透光折痕、page-inner 左右不对称渐变阴影、虚线分隔、楷体 KaiTi）。游戏页模板本来就引了 notes.css，但为避免翻页 JS 类名耦合，独立写 .nb-* 类。
- 左页=打烊小结（点线引导账目 nb-ledger + 进度条 + 便签 + 落款"今天也辛苦了"），右页=深夜订货（nb-scroll 内滚 + 开门按钮钉底）。JS 零改动（ID 全保留）。窄屏 <780px 双页上下叠放。
- 教训复用：版本号 20260906-book 强刷；改完 grep 模板标记数（这次一把过）。

## 深夜订货改表格制（commit da19e55，122/122 测试）
- 用户拿进货系统截图要求：右页改横排表格。落地：nr-table（色块名称|供货价|仓库库存|上期销量|步进器-份数+），底部合计栏+清空+开门按钮；运输中订单挪左页。
- 数据：新增 stats.productSales（commitReservedSale 累计、advanceDay 清零），旧档经 base.stats 合并自动兼容。
- 交互模型：步进器份数 = 下单次数（1 份=orderQuantity 件），+ 受 coins 约束禁用；开门营业时 onOpenNewDay(orders) 批量 placeSupplyOrder（try/catch 单份失败跳过）。nightQty Map 存在 ui 闭包，showNightBreak 时 clear。
- 用户说"先做大概后续微调"——下一步等用户指导（列宽/字段/供货价单位等）。

## 分类书签（commit 0c21a12，122/122 测试）
- 用户在深夜订货右页标题下标记位置要求加零食分类书签，点击筛选。落地：PRODUCTS 每项加 category（sweet/puffed/bake/drink），PRODUCT_CATEGORIES 导出（含"全部"）；renderNightBookmarks + nightCategory 状态；点击委托 data-night-category；CSS 燕尾书签 clip-path polygon(0 0,100% 0,100% 100%,50% 82%)，选中 translateY(5px)+橙渐变沉入页口。切换书签保留已勾份数；空分类显示占位。版本 20260906-bookmark。
- 用户反馈书签要方形：去 clip-path 燕尾，改普通方签（边框+上圆角，选中橙底），版本 20260906-bm2
- hover 闪烁修复（commit 见上）：定时 refreshUI 每 250ms 重建书签/表格 DOM，hover 状态丢失导致闪。修复：书签只在 showNightBreak/点击时渲染；renderNightTable 加 nightSig 签名（分类+金币+勾选份数+库存+销量），不变则跳过 innerHTML 重建。通用教训：任何被定时 render 重建的 DOM 都不能有 :hover/动画态。
- 手机分类条（commit 见上）：data-phone-cats 导航 ×2（库存/配送），共用 phoneCategory；点击只重渲染 renderPhoneCats+renderInventory+renderSupply(Date.now())，不进定时刷新。验证方法：seed iframe + setTimeout 连环 click（phone-button → app → 分类），虚拟时间下可行。

- 2026-09-05 晚：手机二级页左上角「‹ 主屏」改为「返回」（index.html 5 处，版本 v=20260906-back），commit 8fdc131。
- 2026-09-05 晚②：HUD 摘除「季节·时段 时钟」徽标和「店内顾客 n/上限」计数（时间/季节由画面表现：窗外天色+墙上挂钟+全场景色调+植物叶色/天气粒子）；顾客上限设计=招牌等级 min(5, 2+traffic) × 时段 crowdFactor（中午1.2→满级中午可到6）。commit 4d58e22，122 测试通过。
- 2026-09-05 晚③：移除点击顾客查看信息功能（customer-order 气泡面板、selectedCustomerId 全链路、顾客选中高亮框、相关 CSS），点顾客改为不响应；顾客头顶需求气泡保留。commit 后 122 测试通过。
- 2026-09-05 晚④：货架补货气泡重做（木框纸面+虚线标题+羊皮纸状态胶囊+紧凑库存块+绿色像素补货按钮），去掉毛玻璃白卡与蓝色现代风；探针页(同源 /probe.html 注入 pointerdown)截图验证后删除。commit 见 git log。
- 2026-09-05 晚⑤：补货气泡改为行卡制（4 行各一张卡：色块+下拉选种类+货架/仓库库存+「+1/+5/补满」），删掉顶部「补1件/补5件/补满」绿色按钮区与主商品库存块；状态胶囊取整柜最差行；restock-one 等旧元素与监听移除，集成测试同步更新。122 测试通过。
- 2026-09-05 晚⑥：修复下拉选种类被 250ms 刷新关掉——renderShelf 行卡加结构签名（slot+rows+解锁数，不含库存），库存文案/按钮置灰改为 querySelector 定点更新；探针验证 3.5s 内 select 节点 STABLE。commit 2adc0c8，122 测试通过。
- 2026-09-05 晚⑦：手机「配送中心」每行副标题追加「仓库 N」和有在途时的「在途 M」（汇总该零食运输中订单数量）。commit d77ffc0，122 测试通过。
- 2026-09-05 晚⑧：主页页头左侧新增登录便签（sticky-login 粉色，与右侧黄签对称）——输入名字签到存 localStorage(tongsheng-visitor)，再次访问显示「你好呀，XX～」+可换名，顺手预填留言板昵称；小屏(≤640px)隐藏。home.mjs v=20260906。
- 2026-09-05 晚⑨：主页登录便签两处调整——left 6→16px 离边缘留白；签到后隐藏「来客登记」标题。顺带修两个 bug：①.sticky-login form 的 display:flex 压过 hidden 属性（补 [hidden]{display:none!important}）；②loginTitle 声明在 if 块内导致模块级函数 ReferenceError（提到顶层）。home.mjs v=20260907b，commit 7fc4fec。
- 2026-09-05 晚⑩：用户系统 v1（commit 5165fc2）。DB：migration-v5.sql 建 user + game_save 表（已在本机 MySQL 执行）。后端：User/GameSave 实体+Mapper、UserService（BCrypt+用户名校验 2-16位中英数下划线）、AuthApiController（/api/auth/me|signin|logout，signin 一个入口「新名字注册/老名字验密」）、GameSaveApiController（/api/game/save GET/PUT，256KB 上限，云档优先于本地）、HomeController 注入 visitors(12)+visitorCount。前端：主页粉签加密码框（home.mjs v=20260907b→实际页面 fetch /api/auth/me 恢复会话）、来客名册区块、游戏 main.mjs 顶层 await 拉云档（v=20260907-account）+ 玩家名牌 chip + saveGame 防抖3s PUT 云端（“已同步云端 ☁”）。curl 全链路验证：注册/会话/云存档读写/错密码401/未登录401/非法用户名/主页名册渲染 ✓，122 测试通过。重启方式同 sb-run5.log（classworlds 后台任务）。
- 2026-09-05 晚⑪：登录交互重做（commit 5a751cd）——粉签简化为「留下你的名字+签到」，点签到弹屏幕中央双便签弹层（绿=新客登记/粉=老客回来，.is-front/.is-back + flip-to-front-reg/login 关键帧翻页，点后面的签翻到前）；便签 left 16→32px；移除来客名册（模板/CSS/HomeController 注入全清）。HomeController 回滚为不需要重启的改动，静态资源 target 同步即生效。探针验证 FLIP-OK + 截图。home.mjs v=20260907c，122 测试通过。
- 2026-09-05 晚⑫：双便签改真前后叠放（两签同居中，后签 rotate ±9deg + translate 只露一角；翻页关键帧起止与前后态对齐），替代原并列布局。commit 见 git log。
- 2026-09-05 晚⑬：弹层 ✕ 关闭钮从悬空大圆改成钉在前签右上角的小半透明圆点（22px，hover 变粉）。commit 见 git log。
- 2026-09-05 晚⑭：双便签露角改到左上（后签 rotate ∓6deg + translate(-24,-18)，绿/粉翻面后都从左上露），✕ 关闭钮作为「钉子」钉在左上重叠点（top32/left=前签左角+6px），翻页后位置不动；翻页关键帧起止对齐新前后态。无头截图遇 opacity 动画冻帧伪影（记忆中已知坑），探针注入 *{animation:none} 后验证两状态。commit 见 git log。\n- 2026-09-05 晚⑮：按用户草图把钉子(✕)从「前签角内侧」改钉到「后签露出的左上角尖」——后签两种统一 rotate(6deg) translate(-30px,-22px)（露角更大且翻面前后角尖位置一致），✕ 定位 top15/left calc(50- 2026-09-05 晚⑮：按用户草图把钉子(✕)从「前签角内侧」改钉到「后签露出的左上角尖」——后签两种统一 rotate(6deg) translate(-30px,-22px)（露角更大且翻面前后角尖位置一致），✕ 定位 top:15px / left calc(50% 减 156px)；关键帧同步。探针禁动画截两态验证。commit 见 git log。
- 2026-09-05 晚⑯：真正理解用户草图——两签左上角「重合」于同一点：.auth-note transform-origin 改 0 0（角点为旋转轴），后签统一 rotate(-9deg) 绕角扇形展开（沿顶边向右上露出大楔形），✕ 钉子钉在重合角点上（top15 / left calc(50% 减 136px)）；翻页=绕同一角从 -9deg 掀到 ∓2deg。探针禁动画截两态验证。commit 见 git log。
