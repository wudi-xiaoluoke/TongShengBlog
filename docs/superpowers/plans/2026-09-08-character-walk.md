# 顾客四方向序列帧

用户要求：修正长短腿，提供上/下/左/右每方向一组序列帧图片并完善实际移动动画。

- [ ] 新建 game-character.mjs：统一 32×40 透明帧、脚底锚点 (16,34)、四方向各八帧、80ms/帧；两腿尺寸恒定，通过位移和前后遮挡迈步。停步使用单独站立帧，取货/结账使用伸手帧。
- [ ] 学生、邻居、职员、店主四套配色，保留 Q 版比例与饰物；序列导出从同一 Canvas 原稿生成，避免图片与运行时不一致。
- [ ] 生成每角色 4×8 行走 PNG、四方向条图、atlas、idle/reach PNG 与 JSON 锚点信息；游戏加载 atlas，所有人物共用缓存，失败仍可由原重试入口恢复。
- [ ] 增加独立动画验收页：方向、角色、速度、暂停、逐帧查看、全部序列对照。全程不读写游戏存档。
- [ ] 测试固定腿长、脚底界限、循环、待机、镜像、PNG尺寸/透明度和实际加载；检查场景遮挡、气泡、VIP/耐心/收银指示。
- [ ] 完成浏览器与全量单测，同步用户授权目录以及 target/classes；刷新模块版本并提供动画验收链接与图片目录。
